jest.mock('@backstage/core-plugin-api', () => {
  const originalModule = jest.requireActual('@backstage/core-plugin-api');
  return {
    ...originalModule,
    useApi: jest.fn(() => ({
      getInfo: jest.fn(), // Mock the getInfo function
    })),
    devToolsApiRef: {}, // Mock devToolsApiRef
  };
});